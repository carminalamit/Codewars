function printArray(array) {
  return array.join(",");
}

// join() method is used to concatenate all the elements of an array into a string. 
// By default, join() uses a comma as the separator between elements